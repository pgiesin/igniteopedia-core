defmodule AccountManagerTest do

  use ExUnit.Case
  doctest AccountManager

  # alias AccountManager

  # import AccountManager.AccountsFixtures
  # alias RepoManager.Impl.{User, UserToken}

  # describe "get_user_by_email/1" do
  #   test "does not return the user if the email does not exist" do
  #     refute AccountManager.get_user_by_email("unknown@example.com")
  #   end

  #   test "returns the user if the email exists" do
  #     %{id: id} = user = user_fixture()
  #     assert %User{id: ^id} = AccountManager.get_user_by_email(user.email)
  #   end
  # end
end
