import Config

config :account_persistence, AccountPersistence.Runtime.Repo,
        database: "igniteopedia_dev",
        hostname: "localhost",
        username: "postgres",
        password: "hipm3fvn"
